<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FoodDelivery" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_gray</item>
        <item name="colorPrimaryVariant">@color/dark_gray</item>
        <item name="colorOnPrimary">@color/white</item>
        "Theme.AppCompat.Light.NoActionBar"
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/dark_gray</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
    </style>


    <style name="ThemeOverlay.search" parent="Theme.FoodDelivery">
        <!-- Text color -->
        <item name="android:editTextColor">@color/dark_blue</item>
        <!-- Hint text color -->
        <item name="android:textColorHint">@color/dark_gray</item>
        <item name="android:fontFamily">@font/font_2</item>
        <item name="android:textSize">12sp</item>
        <item name="android:searchIcon">@drawable/ic_search</item>
        <item name="tint">@color/dark_gray</item>
    </style>


</resources>